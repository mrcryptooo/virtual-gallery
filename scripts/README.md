# scripts/

Asset pipeline and CI gates land here per [docs/12](../docs/12-implementation-plan-phase-0-1.md):

- `validate-packages.mjs` — M0.4 (manifest/content gates, ADR-010)
- `build-package.mjs` — M0.5/M0.6 (equirect → tiled cube-map project package, doc 07)
- `pick-hotspots.mjs` — M2.x (yaw/pitch picker)
- `perf-budget.mjs` — M1.7 (doc 08 budgets)

Empty at M0.0 by design.
