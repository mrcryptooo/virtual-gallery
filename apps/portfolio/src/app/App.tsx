/**
 * Bootstrap placeholder (M0.0). Product UI begins at M0.3 (design tokens)
 * and is built strictly from docs/11-design-system.md.
 */
export function App() {
  return (
    <main>
      <h1>Virtual Gallery</h1>
      <p>Repository bootstrap — no application UI yet (milestone M0.0).</p>
    </main>
  );
}
